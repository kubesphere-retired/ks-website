webpackJsonp([0x6233d5a45f16],{386:function(e,t){e.exports={data:{allMarkdownRemark:{edges:[{node:{id:"/Users/leoliu/Documents/KubeSphere/kubesphere.github.io/content/install/en/all-in-one.md absPath of file >>> MarkdownRemark",fields:{slug:"/install/en/all-in-one/",framework:"install",language:"en",article:"all-in-one"},html:'<h3>All-in-one Mode</h3>\n<p><strong><code>All-in-one</code></strong> mode means single node deployment,  can be deployed to a single system for a test or development environment only, it is just recommended to get familiar with installation process or learn about KubeSphere features by following with All-in-one mode, which means it can help you to get KubeSphere platform up and running to try out for the first time.  All-in-one mode is not considered a production environment. For production environment, it is recommended to select <strong><code>Multi-node</code></strong> instead.</p>\n<hr>\n<h4>Step 1: Provision Linux Host</h4>\n<h5>Prerequisites</h5>\n<p>The following section identifies the hardware specifications and system-level requirements of one host within KubeSphere platform environment. To get started with All-in-one mode, you may need to prepare only one host refer to the following specification.</p>\n<h5>Hardware Recommendations</h5>\n<table>\n<thead>\n<tr>\n<th>System</th>\n<th>Minimum Requirements</th>\n<th>Recommendations</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>ubuntu 16.04 LTS 64bit</td>\n<td>CPU：8 Core \n<br/>\n Memory：12G \n<br/>\n Disk Space：40G</td>\n<td>CPU：16 Core \n<br/>\n Memory：32G \n<br/>\n Disk Space：100G</td>\n</tr>\n</tbody>\n</table>\n<p>All components of the platform are distributed and installed from the same host, taking 192.168.0.10 as an example.</p>\n<table>\n<thead>\n<tr>\n<th><strong>Host IP</strong></th>\n<th><strong>Roles</strong></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>192.168.0.10</strong></td>\n<td><strong>master , node , etcd</strong></td>\n</tr>\n</tbody>\n</table>\n<h4>Step 2: Provision Installation Files</h4>\n<p><strong>1.</strong>  <strong>KubeSphere</strong> official download address：</p>\n<p><a href="https://drive.yunify.com/s/jV8QSnO8KkWLu4V">KubeSphere Installer download</a> （To be updated）</p>\n<p><strong>Note：</strong>  <br/></p>\n<blockquote>\n<p>If you can not connect to network, please refer to <a href="#disconnected-installation-instructions-to-be-defined">Disconnected Installation Instructions</a></p>\n</blockquote>\n<p><strong>2.</strong>  When you get the installation package, please execute following commands to unzip the package.</p>\n<pre><code>  $ tar -zxvf kubesphere-all-express-1.0.alpha.tar.gz\n</code></pre>\n<p><strong>3.</strong> Go into “<code>kubesphere-express-1.0.alpha</code>” folder</p>\n<pre><code>  $ cd kubesphere-express-1.0.alpha\n</code></pre>\n<p><strong>Reminder：</strong>  <br/></p>\n<blockquote>\n<p>If you would like to customize the configuration parameters, such as network, storage classes, etc. You will be able to specify the parameters in  <strong><code>vars.yml</code></strong> via Vim.</p>\n</blockquote>\n<pre><code>  $ vi conf/vars.yml\n</code></pre>\n<p><strong>Note：</strong>  <br/></p>\n<blockquote>\n<ul>\n<li>You will be able to modify the relevant configurations like network or storage classes, otherwise it will be executed with default parameters without any modifications**</li>\n</ul>\n</blockquote>\n<blockquote>\n<ul>\n<li>Network：KubeSphere supports <code>calico</code> by default**</li>\n</ul>\n</blockquote>\n<blockquote>\n<ul>\n<li>Supported Storage Classes：<code>GlusterFS、CephRBD、local\\_volume(Default)</code>. For details on storage configuration, please refer to <a href="#storage-configuration-instructions">Storage Configuration Instructions</a></li>\n</ul>\n</blockquote>\n<blockquote>\n<ul>\n<li>All-in-One uses local storage as the storage class by default. Since local storage does not support dynamic provisioning, users may need to create a persistent volume (PV) in advance when creating volumes in the KubeSphere console.</li>\n</ul>\n</blockquote>\n<h4>Step 3: Get Started With Deployment</h4>\n<p>The environment and file monitoring, dependent software installation of KubeSphere, automated installation of Kubernetes and etcd, and automated storage configuration, all of these procedures will be automatically processing in this deployment.</p>\n<p>The KubeSphere installation package will automatically install the relevant dependent software as following table described.</p>\n<h5>Software Recommendations</h5>\n<table>\n<thead>\n<tr>\n<th><strong>Dependent Software</strong></th>\n<th><strong>Supported Version</strong></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Ansible</td>\n<td>v2.4 (or newer)</td>\n</tr>\n<tr>\n<td>Python-netaddr</td>\n<td>v0.7.18(or newer)</td>\n</tr>\n<tr>\n<td>Jinja</td>\n<td>v2.9 (or newer)</td>\n</tr>\n</tbody>\n</table>\n<p>Following section describes how to get started with All-in-one mode deployment.</p>\n<blockquote>\n<p>The current system is Ubuntu 16.04. It is recommended to use ubuntu as default user to complete following steps.</p>\n</blockquote>\n<p><strong>1.</strong> Go into <code>scripts</code>(If current location is in kubesphereinstaller)</p>\n<pre><code>$ cd scripts\n</code></pre>\n<p><strong>2.</strong> Execute <strong><code>install.sh</code></strong>：</p>\n<pre><code>$ ./install.sh\n</code></pre>\n<p><strong>3.</strong> Enter<code>1</code> to select <code>All-in-one</code> mode to start：\n<img src="../images/pic06.png"></p>\n<p><strong>Attention：</strong> <br/></p>\n<blockquote>\n<p>If any errors occur, you can check according to the error message, please give us feedback as well.</p>\n</blockquote>\n<p><strong>4.</strong> Test <strong>KubeSphere</strong> All-in-one mode Deployment：</p>\n<p><strong>(1).</strong>If you can see the following "Successful" result being returned after <code>install.sh</code> completed, that means KubuSphere installation is ready.</p>\n<p><img src="../images/pic01.png"></p>\n<p><strong>(2).</strong>Then you will be able to access KubeSphere login page with the IP address of the host and correct port. The port number will be automatically generated in the result page as above screenshot showing "ks-console-nodeport:32117". <br/>\n<strong>Example</strong>： <a href="http://139.198.121.143:31413">http://139.198.121.143:31413</a>\n<br/>\n<img src="../images/pic02.png"></p>\n<h4>Summary</h4>\n<p>When KubeSphere is deployed successfully ，you will be able to follow with <a href="www.qingcloud.com"><em>"<u>KubeSphere User Guide</u>"</em></a>，then log in to KubeSphere and learn how to get started with it！</p>',frontmatter:{title:""}}},{node:{id:"/Users/leoliu/Documents/KubeSphere/kubesphere.github.io/content/install/en/multi-node.md absPath of file >>> MarkdownRemark",fields:{slug:"/install/en/multi-node/",framework:"install",language:"en",article:"multi-node"},html:'<h3>Multi-node Mode</h3>\n<p><code>Multi-node</code> mode means multiple nodes deployment, for example, single or multiple <code>master(s)</code>, multiple <code>nodes</code>, and multiple  <code>etcd</code> nodes. it is recommended to prepare at least <code>2</code> nodes and deploy to production environment.</p>\n<p><strong>Note：</strong>  <br/></p>\n<blockquote>\n<p>It is recommended to select any one host in the cluster being served as a role of "<code>taskbox</code>" to execute installation task for other hosts before Multi-node deployment, and "<code>SSH Communication</code>" is required to be established between "taskbox" and other hosts.</p>\n</blockquote>\n<hr>\n<h4>Step 1: Provision Linux Host</h4>\n<h5>Prerequisites</h5>\n<p>The following section identifies the hardware specifications and system-level requirements of hosts within KubeSphere platform environment. To get started with Multi-node mode, you may need to prepare at least <code>2</code> hosts refer to the following specification.</p>\n<h5>Hardware Recommendations</h5>\n<table>\n<thead>\n<tr>\n<th><strong>Roles</strong></th>\n<th><strong>Minimum Recommended</strong></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>master</strong></td>\n<td>Operating System：ubuntu16.04\n<br/>\nCPU：To be defined\n<br/>\nMemory：To be defined\n<br/>\nDisk：To be defined</td>\n</tr>\n<tr>\n<td><strong>node</strong></td>\n<td>Operating System：ubuntu16.04\n<br/>\nCPU：To be defined\n<br/>\nMemory：To be defined\n<br/>\nDisk：To be defined</td>\n</tr>\n<tr>\n<td><strong>etcd</strong></td>\n<td>Operating System：ubuntu16.04\n<br/>\nCPU：To be defined\n<br/>\nMemory：To be defined\n<br/>\nDisk：To be defined</td>\n</tr>\n</tbody>\n</table>\n<h4>Step 2: Deployment Scenarios(Example description)</h4>\n<p>Take 192.168.0.10 (node1), 192.168.0.20 (node2) and 192.168.0.30 (node3) as an example.</p>\n<p><strong>Attention: <br/></strong></p>\n<blockquote>\n<p>node1 will be served as a role of "taskbox" to execute installation task, the host name of each node can be customized by the user.</p>\n</blockquote>\n<h5>Cluster Architecture</h5>\n<p><strong>Single master, Single etcd, Multiple nodes</strong>\n<img src="../images/pic04_en.png"></p>\n<p><strong>Note:</strong>  <br/></p>\n<blockquote>\n<p>etcd is a distributed key value store that provides a reliable way to store data across a cluster of machines. The number of etcd node is required at least one. For high availability, it is recommended to deploy multiple etcd nodes, which can makes the Kubernetes cluster more reliable. The number of etcd nodes is recommended to be set to <code>odd number</code>. KubeSphere Express version just temporarily supports single etcd, we are going to support multiple etcd in the next AE version soon.</p>\n</blockquote>\n<table>\n<thead>\n<tr>\n<th><strong>Host IP</strong></th>\n<th><strong>Roles</strong></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>192.168.0.10</td>\n<td>master , node , etcd</td>\n</tr>\n<tr>\n<td>192.168.0.20</td>\n<td>node</td>\n</tr>\n<tr>\n<td>192.168.0.30</td>\n<td>node</td>\n</tr>\n</tbody>\n</table>\n<h4>Step 3: Provision Installation Files</h4>\n<p><strong>1.</strong>  <strong>KubeSphere</strong> official download address：</p>\n<p><a href="https://drive.yunify.com/s/jV8QSnO8KkWLu4V">KubeSphere Installer download</a> （To be updated）</p>\n<p><strong>Note：</strong>  <br/></p>\n<blockquote>\n<p>If you can not connect to network, please refer to <a href="#disconnected-installation-instructions-to-be-defined">Disconnected Installation Instructions</a></p>\n</blockquote>\n<p><strong>2.</strong>  When you get the installation package, please execute following commands to unzip the package.</p>\n<pre><code>  $ tar -zxvf kubesphere-all-express-1.0.alpha.tar.gz\n</code></pre>\n<p><strong>3.</strong> Go into “<code>kubesphere-express-1.0.alpha</code>” folder</p>\n<pre><code>  $ cd kubesphere-express-1.0.alpha\n</code></pre>\n<p><strong>Attention: taskbox is required to configure "SSH password-less login", you can refer to <a href="#ssh-password-less-login-configuration-instructions">SSH Password-less Login Configuration Instructions </a> if needed.</strong></p>\n<p><strong>4.</strong> Execute following command to edit <strong><code>hosts.ini</code></strong> via Vim.：</p>\n<pre><code>$ vi conf/hosts.ini\n</code></pre>\n<p><strong>5.</strong> In order to manage deployment process and target machines configuration, please refer to the following scripts to configure all hosts in <code>hosts.ini</code>,</p>\n<p><strong>Attention：</strong></p>\n<blockquote>\n<p>There are 3 hosts in example and each host name can be customized by the user like "node1", "node2" and etc. "node1" is the taskbox as well as the local host.</p>\n</blockquote>\n<p><strong>Example</strong></p>\n<pre><code>[all]\nnode1  ansible_connection=local local_release_dir={{ansible_env.HOME}}/releases ansible_user=ubuntu ansible_become=yes ansible_become_user=root ansible_become_pass=password\nnode2  ansible_host=192.168.0.20 ip=192.168.0.20 ansible_user=ubuntu ansible_become=yes ansible_become_user=root ansible_become_pass=password\nnode3  ansible_host=192.168.0.30 ip=192.168.0.30 ansible_user=ubuntu ansible_become=yes ansible_become_user=root ansible_become_pass=password\n\n[kube-master]\nnode1\n\n[kube-node]\nnode1\nnode2    \nnode3\n\n\n[etcd]\nnode1\n\n\n[k8s-cluster:children]\nkube-node\nkube-master \n</code></pre>\n<p><strong>Note：</strong> <br/></p>\n<blockquote>\n<ul>\n<li>Each node\'s parameters like Internal IP and its password needs to be filled into [all] field. In this example, since "node1" served as <code>taskbox</code>, it just needs to replace "ansible<em>become</em>pass" with the current host password in the first line.</li>\n<li>Other nodes like "node2" and "node3", for "ansible<em>host" and "ansible</em>become_pass" need to be replaced by their actual Internal IP and host password respectively in [all] field.</li>\n<li>"node1" is served as the taskbox which is to execute installation for whole cluster, as well as the role of master and etcd, so "node1" needs to be filled into [kube-master] and [etcd] field.</li>\n<li>At the same time, for "node1","node2" and "node3", they serve the role of node as well, so all of the hosts name need to be filled into [kube-node] field.</li>\n</ul>\n</blockquote>\n<p><strong>6.</strong> It is extremly recommended to prepare a server for storage service before Multi-node deployment. Then you will need to specify the storage class parameters in  <strong><code>vars.yml</code></strong> via Vim. Execute following command to edit <code>vars.yml</code>:</p>\n<pre><code>  $ vi conf/vars.yml\n</code></pre>\n<p><strong>Note：</strong>  <br/></p>\n<blockquote>\n<ul>\n<li>You may to modify the relevant configurations like network or storage class, otherwise it will be executed with default parameters without any modifications**</li>\n</ul>\n</blockquote>\n<blockquote>\n<ul>\n<li>Network：KubeSphere supports <code>calico</code> by default</li>\n</ul>\n</blockquote>\n<blockquote>\n<ul>\n<li>Supported Storage Classes：<code>GlusterFS、CephRBD</code></li>\n</ul>\n</blockquote>\n<blockquote>\n<p><strong>For details on storage configuration, please refer to <a href="#storage-configuration-instructions">Storage Configuration Instructions</a></strong>  </p>\n</blockquote>\n<h4>Step 4: Get Started With Deployment</h4>\n<p>The environment and file monitoring, dependent software installation of KubeSphere, automated installation of Kubernetes and etcd, and automated storage configuration, all of these procedures will be automatically processing in this deployment.</p>\n<p>The KubeSphere installation package will automatically install the relevant dependent software as following table described.</p>\n<h5>Software Recommendations</h5>\n<table>\n<thead>\n<tr>\n<th><strong>Dependent Software</strong></th>\n<th><strong>Supported Version</strong></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Ansible</td>\n<td>v2.4 (or newer)</td>\n</tr>\n<tr>\n<td>Python-netaddr</td>\n<td>v0.7.18(or newer)</td>\n</tr>\n<tr>\n<td>Jinja</td>\n<td>v2.9 (or newer)</td>\n</tr>\n</tbody>\n</table>\n<p>Following section describes how to get started with Multi-node mode deployment.</p>\n<blockquote>\n<p>The current system is Ubuntu 16.04. It is recommended to use ubuntu as default user to complete following steps.</p>\n</blockquote>\n<p>Following section describes how to start <code>Multi-node</code> deployment.</p>\n<p><strong>1.</strong> Go into <code>scripts</code>(If current location is in kubesphereinstaller)</p>\n<pre><code>$ cd scripts\n</code></pre>\n<p><strong>2.</strong> Execute <strong><code>install.sh</code></strong>：</p>\n<pre><code>$ ./install.sh\n</code></pre>\n<p><strong>3.</strong> Enter<code>2</code> to select <code>Multi-node</code> mode to start：\n<img src="../images/pic08.png"></p>\n<p><strong>Attention：</strong> <br/></p>\n<blockquote>\n<p>If any errors occur, you can check according to the error message, please give us feedback as well.</p>\n</blockquote>\n<p><strong>4.</strong> Test <strong>KubeSphere</strong> Multi-node Deployment：</p>\n<p><strong>(1).</strong>If you can see the following "Successful" result being returned after <code>install.sh</code> completed, that means KubuSphere installation is ready.</p>\n<p><img src="../images/pic01.png"></p>\n<p><strong>(2).</strong>Then you will be able to access KubeSphere login page with the IP address of the host and correct port. The port number will be automatically generated in the result page as above screenshot showing "ks-console-nodeport:32117". <br/>\n<strong>Example</strong>： <a href="http://139.198.121.143:31413">http://139.198.121.143:31413</a>\n<br/>\n<img src="../images/pic02.png"></p>\n<h4>Summary</h4>\n<p>When KubeSphere is deployed successfully ，you will be able to follow with <a href="www.qingcloud.com"><em>"KubeSphere User Guide"</em></a>，then log in to KubeSphere and learn how to get started with it！</p>\n<hr>\n<h2>Appendix</h2>\n<h3><strong>Disconnected Installation Instructions</strong>  (To be defined)</h3>\n<p>To be defined</p>\n<hr>\n<h3><strong>Storage Configuration Instructions</strong></h3>\n<h4>Storage classes configuration</h4>\n<p>KubeSphere supports GlusterFS or CephRBD to serve as persistent volume plugin in Multi-node mode deployment, <strong>you will need to prepare the corresponding storage server first.</strong></p>\n<p>You just need to specify the storage class like Ceph RBD , GlusterFS or local volume at <code># Ceph_rbd deployment</code> , <code># GlusterFS provisioner deployment</code> or <code># Local volume provisioner deployment(Only all-in-one)</code> part in <code>vars.yml</code> according to the storage server configuration. Please reference following table and refer to the example hints in <code>vars.yml</code> to modify the parameters for storage class configuration</p>\n<p><strong>Note:</strong><br/></p>\n<blockquote>\n<p>KubeSphere installation will automatically install the on-demand storage client like Ceph RBD Client or GlusterFS Client in Kubernetes cluster, which depends on the storage server configuration, no need to install the client manually.</p>\n</blockquote>\n<p>If you would like to use <code>local\\volume</code>, it is recommended to mount a volume that meets the capacity in this path:（ <em><strong>local\\volume\\provisioner\\base\\dir</strong></em>）\n<br/>\n<strong>Note:</strong> <br/></p>\n<blockquote>\n<p><code>local\\volume</code> is only applicable in All-in-one mode</p>\n</blockquote>\n<p>For <code>Ceph</code> deployment, please refer to <a href="http://docs.ceph.com/docs/master/"> <strong>Install Ceph</strong> </a> or  <a href="https://github.com/ceph/ceph-docker"> <strong>Containerized Ceph cluster</strong> </a></p>\n<p>For <code>Gluster</code>deployment, please refer to <a href="https://www.gluster.org/install/"><strong>Install Gluster</strong></a> or <a href="http://gluster.readthedocs.io/en/latest/Install-Guide/Install/"><strong>Gluster Docs</strong></a>, and it requires <a href="https://github.com/heketi/heketi/tree/master/docs/admin">Heketi</a> for management.</p>\n<p><strong>Attention:</strong><br/></p>\n<blockquote>\n<p>Kubernetes is not allowed exits both 2 kinds of storage class at the same time,  please make sure there is not a default storage class in Kubernetes cluster before you are going to specify the storage class. </p>\n</blockquote>\n<p>Following tables describe different storage classes parameters specification, For <code>Storage Class</code> details please refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/"><strong>storage classes</strong></a> as well.</p>\n<table>\n<thead>\n<tr>\n<th><strong>Local volume</strong></th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>local_volume_provisioner_enabled</strong></td>\n<td><strong>Whether to use "local_volume" as persistent volume plugin, Yes:"true" No:"false"</strong></td>\n</tr>\n<tr>\n<td><strong>local_volume_provisioner_storage_class</strong></td>\n<td><strong>storage_class name,   Default value：local-storage</strong></td>\n</tr>\n<tr>\n<td><strong>local_volume_provisioner_namespace</strong></td>\n<td><strong>the namespace of relevant local_volume object,  Default value：kube-system</strong></td>\n</tr>\n<tr>\n<td><strong>local_volume_is_default_class</strong></td>\n<td><strong>Whether to set it as default storage_class, Yes:"true" No:"false"<br/> Attention：It is  allowed to set only one kind of storage class as default_class</strong></td>\n</tr>\n<tr>\n<td><strong>local_volume_provisioner_base_dir</strong></td>\n<td><strong>Host mounted path,  Default value：/mnt/disks</strong></td>\n</tr>\n</tbody>\n</table>\n<br/>\n<table>\n<thead>\n<tr>\n<th><strong>Ceph_RBD</strong></th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>ceph_rbd_enabled</strong></td>\n<td><strong>Whether to use ceph_RBD as persistent storage volume,   Yes:"true" No:"false"</strong></td>\n</tr>\n<tr>\n<td><strong>ceph_rbd_storage_class</strong></td>\n<td><strong>storage_class name</strong></td>\n</tr>\n<tr>\n<td><strong>ceph_rbd_is_default_class</strong></td>\n<td><strong>Whether to set it as default storage_class, Yes:"true" No:"false" <br/> Attention：It is  allowed to set only one kind of storage class as default_class</strong></td>\n</tr>\n<tr>\n<td><strong>ceph_rbd_monitors</strong></td>\n<td><strong>Ceph monitors, comma delimited. This parameter is required, which depends on Ceph RBD server parameters. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#ceph-rbd">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>ceph_rbd_admin_id</strong></td>\n<td><strong>Ceph client ID that is capable of creating images in the pool. Default is “admin”. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#ceph-rbd">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>ceph_rbd_admin_secret</strong></td>\n<td><strong>Admin_id\'s secret，Secret Name for "adminId". This parameter is required. The provided secret must have type “kubernetes.io/rbd”. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#ceph-rbd">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>ceph_rbd_pool</strong></td>\n<td><strong>Ceph RBD pool. Default is “rbd”. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#ceph-rbd">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>ceph_rbd_user_id</strong></td>\n<td><strong>Ceph client ID that is used to map the RBD image. Default is the same as adminId，Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#ceph-rbd">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>ceph_rbd_user_secret</strong></td>\n<td><strong>secret for User_id, it is required to create this secret in namespace which used rbd image，Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#ceph-rbd">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>ceph_rbd_fsType</strong></td>\n<td><strong>fsType that is supported by kubernetes. Default: "ext4". Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#ceph-rbd">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>ceph_rbd_imageFormat</strong></td>\n<td><strong>Ceph RBD image format, “1” or “2”. Default is “1”. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#ceph-rbd">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>ceph_rbd_imageFeatures</strong></td>\n<td><strong>This parameter is optional and should only be used if you set imageFormat to “2”. Currently supported features are layering only. Default is “”, and no features are turned on. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#ceph-rbd">Kubernetes Docs</a></strong></td>\n</tr>\n</tbody>\n</table>\n<p><strong>Attention:</strong><br/></p>\n<blockquote>\n<p>The on-demand ceph secret which being created in storage class, like "ceph<em>rbd</em>admin<em>secret" and "ceph</em>rbd<em>user</em>secret", it can be returned with following command in Ceph storage server.</p>\n</blockquote>\n<pre><code>$ ceph auth get-key client.admin\n</code></pre>\n<br/>\n<table>\n<thead>\n<tr>\n<th><strong>GlusterFS（It requires glusterfs cluster which is managed by heketi）</strong></th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>glusterfs_provisioner_enabled</strong></td>\n<td><strong>Whether to use glusterfs as persistent storage volume,    Yes:"true" No:"false"</strong></td>\n</tr>\n<tr>\n<td><strong>glusterfs_provisioner_storage_class</strong></td>\n<td><strong>storage_class name</strong></td>\n</tr>\n<tr>\n<td><strong>glusterfs_is_default_class</strong></td>\n<td><strong>Whether to set it as default storage_class, Yes:"true" No:"false" <br/> Attention：It is  allowed to set only one kind of storage class as default_class</strong></td>\n</tr>\n<tr>\n<td><strong>glusterfs_provisioner_restauthenabled</strong></td>\n<td><strong>Gluster REST service authentication boolean that enables authentication to the REST server. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#glusterfs">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>glusterfs_provisioner_resturl</strong></td>\n<td><strong>Gluster REST service/Heketi service url which provision gluster volumes on demand. The general format should be IPaddress:Port and this is a mandatory parameter for GlusterFS dynamic provisioner. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#glusterfs">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>glusterfs_provisioner_clusterid</strong></td>\n<td><strong>Optional, for example,630372ccdc720a92c681fb928f27b53f is the ID of the cluster which will be used by Heketi when provisioning the volume. It can also be a list of clusterids. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#glusterfs">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>glusterfs_provisioner_restuser</strong></td>\n<td><strong>Gluster REST service/Heketi user who has access to create volumes in the Gluster Trusted Pool. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#glusterfs">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>glusterfs_provisioner_secretName</strong></td>\n<td><strong>Optional, identification of Secret instance that contains user password to use when talking to Gluster REST service,Installation package will automatically create this secret in Kube-system，Refer to<a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#glusterfs">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>glusterfs_provisioner_gidMin</strong></td>\n<td><strong>The minimum value of GID range for the storage class. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#glusterfs">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>glusterfs_provisioner_gidMax</strong></td>\n<td><strong>The maximum value of GID range for the storage class. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#glusterfs">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>glusterfs_provisioner_volumetype</strong></td>\n<td><strong>The volume type and its parameters can be configured with this optional value,For example: ‘Replica volume’: volumetype: replicate:3. Refer to <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#glusterfs">Kubernetes Docs</a></strong></td>\n</tr>\n<tr>\n<td><strong>jwt_admin_key</strong></td>\n<td><strong>"jwt.admin.key" column from "/etc/heketi/heketi.json" in Heketi server</strong></td>\n</tr>\n</tbody>\n</table>\n<p><strong>Attention：</strong><br/></p>\n<blockquote>\n<p>In Glusterfs, <code>"glusterfs_provisioner_clusterid"</code> could be returned from glusterfs server. Execute following command:</p>\n</blockquote>\n<pre><code>$ export HEKETI_CLI_SERVER=http://localhost:8080\n\n$ heketi-cli cluster list\n</code></pre>\n<h4>How to use local volume(It does not belong to Kubesphere Installation)</h4>\n<p>This section does not belong to Kubesphere Installation, we just plan to guide users how to use local volume.</p>\n<blockquote>\n<p>local volume is only applicapable for <code>All-in-one</code> mode.</p>\n</blockquote>\n<p>Following steps describes how to use <code>local volume</code>：</p>\n<p>1.Create folder in host first</p>\n<p>2.Create Persistent Volume</p>\n<p>3.Create Volume in KubeSphere</p>\n<ul>\n<li>Log in to host and create folder first，take an example as following section.Execute following command：</li>\n</ul>\n<pre><code>mkdir -p /mnt/disks/vol-test\n</code></pre>\n<ul>\n<li>\n<p>Create Persistent Volume</p>\n<ul>\n<li>pv.yaml file specification</li>\n</ul>\n</li>\n</ul>\n<pre><code>apiVersion: v1\nkind: PersistentVolume\nmetadata:\n  name: pv-local\nspec:\n  capacity:\n    storage: 10Gi \n  # volumeMode field requires BlockVolume Alpha feature gate to be enabled.\n  volumeMode: Filesystem\n  accessModes:\n  - ReadWriteOnce\n  persistentVolumeReclaimPolicy: Delete\n  storageClassName: local\n  local:\n    path: /mnt/disks/vol-test\n  nodeAffinity:\n    required:\n      nodeSelectorTerms:\n      - matchExpressions:\n        - key: kubernetes.io/hostname\n          operator: In\n          values:\n          - kubesphere\n</code></pre>\n<ul>\n<li>Execute creation command:</li>\n</ul>\n<pre><code>$ kubectl create -f pv-local.yaml\n</code></pre>\n<ul>\n<li>Execute following command to test creation result:</li>\n</ul>\n<pre><code>$ kubectl get pv\nNAME         CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS      CLAIM        STORAGECLASS    REASON    AGE\npv-local     10Gi       RWO            Delete           Available                local                     4s\n</code></pre>\n<ul>\n<li>Then you may need to create local volume in KubeSphere UI：</li>\n</ul>\n<p><strong>Note：</strong> <br/></p>\n<blockquote>\n<p> It is normal when the status is "Pending" after Local volume successfully created, the volume status will change to "Bound" until workload being created after pod scheduling.</p>\n</blockquote>\n<ul>\n<li>\n<p>Following section describes how to clean up volume:</p>\n<ol>\n<li>\n<p>Delete the unmounted volumes in Kubesphere UI</p>\n</li>\n<li>\n<p>Log in to host and delete persistent volumes</p>\n</li>\n<li>\n<p>Remove the folders related to persistent volumes for data clean-up.</p>\n</li>\n</ol>\n</li>\n</ul>\n<blockquote>\n<p>Local volumes do not support dynamic provisioning yet, if you would like to experience dynamic provisioning recommended by KubeSphere, please configure the storage server for GlusterFS or Ceph RBD.</p>\n</blockquote>\n<hr>\n<h3><strong>SSH Password-less Login Configuration Instructions</strong></h3>\n<p>Execute following commands orderly，go into  <strong><code>scripts</code></strong> ，then execute <strong><code>sendsshkey.py</code></strong> :</p>\n<pre><code>$ cd scripts\n</code></pre>\n<pre><code>$ ./sendsshkey.py\n</code></pre>\n<p>If returns  <strong>"<code>id\\_rsa.pub Does not exist!</code>"</strong>  ,  please generate SSH key firstly ，then execute <code>sendsshkey.py</code> again.</p>\n<p><strong>Generate SSH key ：</strong></p>\n<pre><code>$ ssh-keygen -t rsa -N \'\'\n</code></pre>',frontmatter:{title:""}}}]}},pathContext:{framework:"install",language:"en"}}}});